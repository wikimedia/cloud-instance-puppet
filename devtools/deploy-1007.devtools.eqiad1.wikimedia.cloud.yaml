profile::confd::instances:
  main:
    interval: 300
profile::mediawiki::scap_client::is_master: true
profile::pki::client::ensure: present
profile::tlsproxy::envoy::global_cert_name: deploy-1007.devtools.eqiad1.wikimedia.cloud
profile::tlsproxy::envoy::ssl_provider: cfssl
