kafka_clusters:
  beta_logging:
    brokers:
      logging-kafka-01.logging.eqiad1.wikimedia.cloud:
        id: 1001
        rack: A
    zookeeper_cluster_name: beta_logging
profile::kafka::broker::kafka_cluster_name: beta_logging
profile::zookeeper::cluster_name: beta_logging
profile::zookeeper::zookeeper_version: 3.4.13-2
zookeeper_clusters:
  beta_logging:
    hosts:
      logging-kafka-01.logging.eqiad1.wikimedia.cloud: '1001'
